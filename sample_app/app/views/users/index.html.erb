<% provide(:title, 'All users') %>
<ul class="users">
  <h2>All users</h2>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
  <%= paginate @users %>
</ul>